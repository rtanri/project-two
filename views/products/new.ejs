<div class="container d-flex flex-column align-items-center">
      <h1 class="mb-4">Upload New Photo</h1>

      <form method="post" action="/beautylash/add-post" >
            <%- include('../partials/input', { name: 'name', placeholder: 'Jessica'})  %>
            <%- include('../partials/input', { name: 'service', placeholder: "Russian Volume Extension"})  %>

            <%- include('../partials/input', { name: 'image', placeholder: "image url"})  %>
            <div class="d-flex flex-row justify-content-center mt-5">
                  <button type="submit" class="btn btn-secondary frontpage-button">Submit</button>
            </div>
      </form>
</div>
